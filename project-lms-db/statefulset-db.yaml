---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongodb-vol
spec:
  selector:
    matchLabels:
      app: mongodb-vol
  serviceName: mongodb-svc
  replicas: 1
  template:
    metadata:
      labels:
        app: mongodb-vol
    spec:
      containers:
        - name: mongodb
          image: mongo
          envFrom:
            - configMapRef:
                name: configmap-mongo
          ports:
            - containerPort: 27017
          volumeMounts:
            - name: csi-pod-pvc
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: csi-pod-pvc
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 4Gi
        storageClassName: ebs-sc
        dataSource: # 기존 PVC의 이름을 지정
          name: csi-pod-pvc-mongodb-vol-0
          kind: PersistentVolumeClaim
